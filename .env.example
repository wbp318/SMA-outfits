# Copy to .env (gitignored) and fill in. Never commit real keys.
# Kraken: create at Settings -> API with QUERY + TRADE permissions only.
# Do NOT enable withdrawal permission.
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# Webull (later, once you have OpenAPI access):
# WEBULL_APP_KEY=
# WEBULL_APP_SECRET=
